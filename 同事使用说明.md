# 言出法随 · 法眼如炬 —— 同事使用说明

> 仓库：https://github.com/15810879921-coder/wangmian-skills  
> 安装中心：https://15810879921-coder.github.io/oneos-pm-skills/  
> 签名：**王冕驱动 · 言出法随** / **王冕驱动 · 法眼如炬**

---

## 1. 这两个 Skill 是干什么的

| Skill | 花名 | Skills ID | 干什么 | 不干什么 |
|--------|------|-----------|--------|----------|
| **言出法随** | 产品分身 | `yanchufasui` | 按王冕的产品习惯整理需求、改原型 UI/UX；需要时问要不要上云效 | 不偷偷建云效单 |
| **法眼如炬** | 口径裁决 | `fayanruju` | 查 OneOS 知识库口径、冲突裁决、能不能做 | **不改原型** |

一句话：

- 要**办事落地**（需求 / 原型）→ 喊 **言出法随**  
- 只问**规则 / 能不能做 / 有没有断头流** → 喊 **法眼如炬**（更快）  
- 两个都喊 → 法眼先裁口径，分身再按习惯落地  

旧口令仍可用（会自动转到新 Skill）：`王冕分身` / `$wangmian-twin`、`分身大脑` / `$wangmian-brain`。

---

## 2. 怎么安装 / 更新（Cursor + Codex）

### 推荐：一键脚本（两个一起装）

在终端执行：

```bash
curl -fsSL https://raw.githubusercontent.com/15810879921-coder/wangmian-skills/main/install.sh | bash
```

或打开安装中心，点「王冕驱动双 skill」→ **复制一键安装脚本**：  
https://15810879921-coder.github.io/oneos-pm-skills/

### 分开装（npx）

```bash
npx skills add 15810879921-coder/wangmian-skills --skill yanchufasui -a cursor -a codex -g -y
npx skills add 15810879921-coder/wangmian-skills --skill fayanruju -a cursor -a codex -g -y
```

### 更新

```bash
# 脚本再跑一遍即更新
curl -fsSL https://raw.githubusercontent.com/15810879921-coder/wangmian-skills/main/install.sh | bash

# 或
npx skills update yanchufasui fayanruju -g -y
```

### 装完必做

1. **新开一条 Chat**（旧会话可能还缓存旧 Skill）  
2. 云效相关操作前，浏览器先登录：https://devops.aliyun.com  
3. 需要冒烟自检时：`RUN_SMOKE=1 bash install.sh`（默认不跑，加快安装）

---

## 3. 第一次怎么喊（按你的角色）

口令激活后，若你不是王冕，分身会先问身份（本会话问一次即可）：

- **研发**  
- **项目经理**  
- **业务测试及其他同事**  

之后若要换身份，说：`我的角色是研发`（或项目经理 / 业务测试及其他同事）。

### 研发同学（推荐用法）

| 你想做什么 | 怎么喊 | 说明 |
|------------|--------|------|
| 这条规则对不对 / 能不能做 | `/fayanruju` 或 `法眼如炬` | **优先**；只裁口径，最快 |
| 要一份给人话 + AI 可读的规程 MD | 先说法眼题，并说明要喂 Codex | 仅「研发 + 规则/能不能做」才出 MD |
| 查项目云效进展 | ❌ 不要用法眼/分身查进展 | 请找项目经理角色或本尊 |

### 项目经理

| 你想做什么 | 怎么喊 |
|------------|--------|
| 排期 / 进展 / 延期归因 | `/yanchufasui`，角色选项目经理 |
| 只核对业务口径 | `/fayanruju` |

写云效仍须确认，不会偷偷建单。

### 业务 / 测试及其他同事

| 你想做什么 | 怎么喊 |
|------------|--------|
| 讲清楚流程、验收怎么点 | `/yanchufasui` 或 `/fayanruju`（只问口径用后者） |
| 改原型 / 落需求说明 | `/yanchufasui`（落盘前会按王冕习惯；动云效要本尊点头） |

---

## 4. 常用口令速查

| 用途 | 推荐口令 |
|------|----------|
| 产品分身 | `言出法随` / `$yanchufasui` / `/yanchufasui` |
| 口径裁决 | `法眼如炬` / `$fayanruju` / `/fayanruju` |
| 旧口令（兼容） | `王冕分身` / `$wangmian-twin`；`分身大脑` / `$wangmian-brain` |

开场签名示例（正常现象）：

- `王冕驱动 · 言出法随就位——主线接了。`  
- `王冕驱动 · 法眼如炬——按本尊口径裁。`

---

## 5. 使用时你会看到什么（预期）

业务相关回答会尽量带齐 **闭环五件套**（本尊工作习惯）：

1. **闭环** — 钱/状态能不能走通  
2. **缺口** — 还缺什么、待拍板什么  
3. **优化** — 不挡主链的改进点  
4. **可行性** — 可做 / 有条件 / 不可做  
5. **更好办法** — 至少一条更优拆法或替代路径  

租赁主链相关口径，当前定版对齐 **v2.5.8f**（客户创建→还车全闭环；细则在仓库法眼摘要与原型 PRD）。

文案习惯：用户可见流程用语用「**审核**」，不用「审批」（代码字段名不动）。

---

## 6. 性能与提速（同事必看）

| 现象 | 建议 |
|------|------|
| 首次安装慢 | 多半是拉 GitHub；装完新开 Chat；脚本默认已跳过冒烟 |
| 一喊分身很慢 | 已改为**瘦启动**（只读启动卡）；请更新到最新版 |
| 研发只问规则仍慢 | **改喊法眼如炬**，不要默认拉满分身 |
| Codex 比 Cursor 慢 | 两边 Skills 目录分开；更新后新开会话；检查公司代理/网络 |

更新命令见上文 §2。

---

## 7. 明确不会做的事

- 未确认就建云效需求 / 推进状态  
- 法眼如炬改原型代码  
- 研发角色用分身去刷云效项目进展  
- 把未拍板能力当成现网强验收  

---

## 8. 遇到问题

1. 再跑一遍安装脚本并 **新开 Chat**  
2. 确认 Skills 里能看到 `yanchufasui` / `fayanruju`  
3. 仍异常：把「用的 Cursor 还是 Codex + 喊的口令 + 现象」发给本尊（王冕）  

更新功能明细见同目录：[更新日志.md](./更新日志.md)
